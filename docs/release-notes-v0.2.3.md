# OpenConnect Android v0.2.3

## English

- Replaced the launcher icon set with the new OpenConnect app icon assets
- Updated the Android manifest to use `@mipmap/ic_launcher` and `@mipmap/ic_launcher_round`
- Added the refreshed `images/logo.png` branding asset to the repository
- Keeps the public repository free of maintainer-only domains, tokens, and private local configuration

## 中文

- 使用新的 OpenConnect 图标资源替换了 Android 启动图标
- Android manifest 已改为引用 `@mipmap/ic_launcher` 和 `@mipmap/ic_launcher_round`
- 将更新后的品牌素材 `images/logo.png` 加入仓库
- 再次确认公开仓库中不包含维护者私有域名、token 或本地私有配置
